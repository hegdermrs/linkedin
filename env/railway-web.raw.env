# Paste into Railway → web service → Variables → Raw Editor
# Redeploy web after changing NEXT_PUBLIC_API_URL (baked in at build time).

NODE_ENV=production

NEXT_PUBLIC_API_URL=https://CHANGE_ME-api.up.railway.app
