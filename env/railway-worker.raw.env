# Paste into Railway → worker service → Variables → Raw Editor
# DEEPSEEK_API_KEY must match api service exactly.

NODE_ENV=production

DATABASE_URL=${{Postgres.DATABASE_URL}}
REDIS_URL=${{Redis.REDIS_URL}}

SESSION_ENCRYPTION_KEY=CHANGE_ME_32_CHAR_RANDOM_STRING_AAA111
DEEPSEEK_API_KEY=sk-CHANGE_ME_DEEPSEEK

DEFAULT_LLM_PROVIDER=deepseek
DEFAULT_LLM_MODEL=deepseek-chat

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
